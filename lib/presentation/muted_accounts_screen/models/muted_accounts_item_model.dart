/// This class is used in the [muted_accounts_item_widget] screen.
class MutedAccountsItemModel {MutedAccountsItemModel({this.gracekelly, this.stylist, this.id, }) { gracekelly = gracekelly  ?? "Grace Kelly";stylist = stylist  ?? "STYLIST";id = id  ?? ""; }

String? gracekelly;

String? stylist;

String? id;

 }
